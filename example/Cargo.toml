[package]
name = "example"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.219", features = ["derive"] }
tokio = { version = "1.45.0", features = ["macros", "rt-multi-thread"] }
tsue = { version = "0.1.0", path = "../tsue", features = ["tokio","log"] }
